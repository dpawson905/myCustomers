<!DOCTYPE html>
<html>

<head>
  <title>
    <%= title %>
  </title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel='stylesheet' href='/stylesheets/main.css' />
  <link rel='stylesheet' href='/stylesheets/materialize.min.css' />
  <link href="https://fonts.googleapis.com/css?family=Lato:400,700|Merriweather:400,700" rel="stylesheet">
  <script src="/javascripts/jquery.min.js"></script>
  <script src="/javascripts/jquery.mask.min.js"></script>
  <script src="/javascripts/materialize.min.js"></script>
  <script src="/javascripts/main.js"></script>

</head>

<body class="hero-image">
  <nav class="transparent z-depth-0">
    <div class="nav-wrapper container">
      <div class="hide-on-small-only"><a href="/" class="brand-logo">
          <%= title %></a></div>
      <a href="#" data-target="mobile-demo" class="sidenav-trigger"><i class="fas fa-bars"></i>menu</i></a>
      <% if(!isAuthenticated) { %>
      <ul class="right hide-on-med-and-down">
        <li class="<%= typeof page !== 'undefined' && page === 'login' ? 'active' : '' %>"><a href="/auth/login"><i
              class="fas fa-sign-in-alt"></i>
            Login</a></li>
        <li class="<%= typeof page !== 'undefined' && page === 'register' ? 'active' : '' %>"><a href="/register"><i
              class="fas fa-user-plus"></i> Register</a></li>
      </ul>
      <% } else if(currentUser.isAdmin) { %>
      <ul class="right hide-on-med-and-down">
        <li class="<%= typeof page !== 'undefined' && page === 'customers' ? 'active' : '' %>"><a href="/customers/search"><i
              class="fas fa-sign-in-alt"></i>
            Search Customers</a></li>
        <li class="<%= typeof page !== 'undefined' && page === 'add' ? 'active' : '' %>"><a href="/customers/add"><i
              class="fas fa-user-plus"></i> New Customer</a></li>
        <li>
          <div class="chip hoverable">
            <img src="<%= currentUser.image %>" alt="Contact Person">
            Hello
            <%= currentUser.firstName.charAt(0).toUpperCase() + currentUser.firstName.slice(1) %>
          </div>
        </li>
        <% if(!currentUser.twilioNumber || currentUser.twilioNumber === "") { %>
        <li class="<%= typeof page !== 'undefined' && page === 'addKey' ? 'active' : '' %>">
          <a href="/auth/user/<%= currentUser.id %>"><i class="fas fa-key"></i> Add Twillow Info</a>
        </li>
        <% } %>
      </ul>
      <% } else { %>
      <ul class="right hide-on-med-and-down">
        <li class="<%= typeof page !== 'undefined' && page === 'customers' ? 'active' : '' %>"><a href="/customers/search"><i
              class="fas fa-sign-in-alt"></i>
            Search Customers</a></li>
        <li class="<%= typeof page !== 'undefined' && page === 'add' ? 'active' : '' %>"><a href="/customers/add"><i
              class="fas fa-user-plus"></i> New Customer</a></li>
        <li>
          <div class="chip hoverable">
            <img src="<%= currentUser.image %>" alt="Contact Person">
            Hello
            <%= currentUser.firstName.charAt(0).toUpperCase() + currentUser.firstName.slice(1) %>
          </div>
        </li>
      </ul>
      <% } %>
    </div>
  </nav>
  <% if(!isAuthenticated) { %>
  <ul class="sidenav" id="mobile-demo">
    <li><a href="sass.html">Sass</a></li>
    <li><a href="badges.html">Components</a></li>
    <li><a href="collapsible.html">Javascript</a></li>
    <li><a href="mobile.html">Mobile</a></li>
  </ul>
  <% } else { %>
  <ul class="sidenav" id="mobile-demo">
    <li>
      <div class="chip move-right-sm">
        <img src="<%= currentUser.image %>" alt="Contact Person">
        Hello
        <%= currentUser.firstName.charAt(0).toUpperCase() + currentUser.firstName.slice(1) %>
      </div>
    </li>
    <li><a href="sass.html">Sass</a></li>
    <li><a href="badges.html">Components</a></li>
    <li><a href="collapsible.html">Javascript</a></li>
    <li><a href="mobile.html">Mobile</a></li>
  </ul>
  <% } %>
  <% include partials/messages %>
  <div class="container h-100 valign-wrapper white-text">
    <div class="row">
      <div class="col m12">
        <% if(!isAuthenticated) { %>
        <h1 class="center">Your Customer Portal</h1>
        <p class="center">Please login to add and search for customers.</p>
        <% } else { %>
        <h1 class="center">Hello
          <%=currentUser.firstName.charAt(0).toUpperCase() + currentUser.firstName.slice(1) %>
        </h1>
        <p class="center">Click on Customers to view all customers or</p>
        <p class="center">Click on New Customer to add a new customer</p>
        <% } %>
      </div>
    </div>
  </div>
  <% include partials/footer %>