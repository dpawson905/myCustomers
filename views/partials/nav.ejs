<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
  <a class="navbar-brand" href="/"><%= title %></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item <%= typeof page !== 'undefined' && page === 'customers' ? 'active' : '' %>">
        <a class="nav-link" href="/customers">Search Customers <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item <%= typeof page !== 'undefined' && page === 'add_customer' ? 'active' : '' %>">
        <a class="nav-link" href="/customers/add">Add New Customer</a>
      </li>
    </ul>
    <% if(!isAuthenticated) { %>
      <form class="form-inline my-2 my-lg-0 mr-2" action="/auth/login" method="POST">
        <input type="text" id="username" name="username" class="form-control mr-1" placeholder="Username" required autofocus>
        <input type="password" id="password" name="password" class="form-control mr-1 mobile" placeholder="Password" required>
        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Login</button>
      </form>
      <a href="/register" class="btn btn-outline-success">Register</a>
    <% } else if(currentUser.isAdmin) { %>
      <ul class="navbar-nav ml-auto">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <img src="<%=currentUser.image %>" class="img-fluid small-img">&nbsp;<%= currentUser.firstName.charAt(0).toUpperCase() + currentUser.firstName.slice(1)%>
            <%= currentUser.lastName.charAt(0).toUpperCase() + currentUser.lastName.slice(1)%>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <a class="dropdown-item" href="#">ACP</a>
            <a class="dropdown-item" href="#">Profile</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="/auth/logout">Logout</a>
          </div>
        </li>
      </ul>
    <% } else { %>
      <ul class="navbar-nav ml-auto justify-content-custom">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <img src="<%=currentUser.image %>" class="img-fluid small-img"> Hello <%= currentUser.firstName.charAt(0).toUpperCase() + currentUser.firstName.slice(1)%>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <a class="dropdown-item" href="#">Profile</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="/logout">Logout</a>
          </div>
        </li>
      </ul>
    <% } %>
  </div>
</nav>